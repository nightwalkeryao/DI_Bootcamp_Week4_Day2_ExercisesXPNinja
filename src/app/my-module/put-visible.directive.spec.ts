import { PutVisibleDirective } from './put-visible.directive';

describe('PutVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new PutVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
